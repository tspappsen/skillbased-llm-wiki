{
  "name": "mcp-knowledge-base",
  "version": "0.1.0",
  "description": "MCP server for persistent knowledge base — writes markdown to ~/.cache/kb/raw/ and indexes with QMD",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
